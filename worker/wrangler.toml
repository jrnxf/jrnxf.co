name = "jrnxf-github-proxy"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Custom domain — requires jrnxf.co zone on this Cloudflare account
# Wrangler will auto-create the DNS record + SSL cert
routes = [
  { pattern = "api.jrnxf.co/*", zone_name = "jrnxf.co" }
]
